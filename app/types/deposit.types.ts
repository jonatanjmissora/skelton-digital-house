export type DepositTypes = {
  amount: number;
  destination: string;
  origin: string;
}

export type DepositDataTypes = {
  account_id: number;
  amount: number;
  dated: string;
  description: string;
  destination: string;
  id: number;
  origin: string;
  type: string;
}